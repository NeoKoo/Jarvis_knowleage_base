# Obsidian 集成设置记录

**日期：** 2026-02-03

---

## 📋 已完成的配置

### 1. Obsidian CLI 安装
```bash
# 已安装的工具
obsidian-cli         # 基础 vault 操作（搜索、创建、移动、删除、打开笔记）
obsidian-daily        # 每日笔记管理
obsidian-conversation-backup  # 对话自动备份
obsidian-tasks       # Obsidian Tasks 集成
obsidian-sync        # 同步管理
obsidian-plugin-dev # 插件开发
obsidian-plugin-tasknotes # Tasknotes 插件
```

### 2. Obsidian Vault 配置
- **配置文件：** `/home/admin/Library/Application Support/obsidian/obsidian.json`
- **Vault 路径：** `/home/admin/clawd/knowledge-base`
- **状态：** 已设置为默认 vault

```json
{
  "vaults": [
    {
      "path": "/home/admin/clawd/knowledge-base",
      "open": true
    }
  ]
}
```

---

## ⚠️ 关于 "打开 Vault" 的说明

### obsidian-cli 中的 open 命令
`obsidian-cli open` 可以打开 vault 中的特定笔记，**但前提是 Obsidian 应用必须已经打开这个 vault**。

### 为什么不能完全自动化？
Obsidian CLI 是通过 Obsidian 的本地 API 工作的，这要求：
1. Obsidian 应用必须**运行**（打开 vault）
2. CLI 才能通过 API 操作 vault

### Linux 服务器的情况
当前环境是 Linux 服务器，Obsidian 是桌面应用（macOS/Windows）。
- **无法直接在服务器上运行 Obsidian**
- obsidian-cli 的 vault 路径设置是有效的，但需要 Obsidian 客户端打开才能工作

---

## 🎯 可用的自动化方案

### 方案 A：半自动化（当前可行）
1. **使用 obsidian-cli create 命令**
   ```bash
   obsidian-cli create "文件名" --content "# 标题\n\n内容"
   ```
   这会直接写入 vault 文件，但笔记不会自动在 Obsidian 中显示（除非 Obsidian 打开）

2. **使用 obsidian-cli search 命令**
   ```bash
   obsidian-cli search "关键词"
   obsidian-cli search-content "关键词"
   ```
   可以搜索现有笔记

3. **使用 obsidian-cli open 命令**
   ```bash
   obsidian-cli open "笔记名"
   ```
   可以打开特定笔记（需 Obsidian 已打开）

### 方案 B：使用 Obsidian 插件（推荐）
在 Obsidian 中安装以下插件：
- **Templater** - 快速创建笔记模板
- **Hotkeys for specific files** - 自定义快捷键
- **Dataview** - 数据库和查询
- **Excalidraw** - 日历集成
- **QuickAdd** - 快速添加笔记

### 方案 C：移动端 Obsidian
在手机/iPad 上安装 Obsidian Mobile，随时随地访问 vault。

### 方案 D：自动同步
使用 **obsidian-sync** 插件或 **Git 同步**：
1. vault 已经在 Git 仓库中
2. 可以在本地编辑笔记
3. 使用 Obsidian Sync 插件自动同步到设备
4. 不需要每次都打开 Obsidian 桌面应用

---

## 📝 常用命令速查

### 搜索笔记
```bash
# 搜索笔记名称
obsidian-cli search "关键词"

# 搜索笔记内容
obsidian-cli search-content "关键词"
```

### 查看配置
```bash
# 打印当前 vault 路径
obsidian-cli print-default --path-only

# 查看完整配置
cat ~/Library/Application\ Support/obsidian/obsidian.json
```

### 创建新笔记
```bash
# 基础创建
obsidian-cli create "标题" --content "# 标题\n\n内容"

# 使用 daily 命令（每日笔记）
obsidian-cli daily "标题" --content "内容"
```

### 移动/重命名笔记
```bash
# 移动笔记（会自动更新 wikilinks）
obsidian-cli move "旧路径" "新路径"
```

### 打开笔记
```bash
# 打开特定笔记（需 Obsidian 打开）
obsidian-cli open "笔记名"
```

---

## 💡 使用建议

### 日常操作流程
1. **创建笔记** → 告诉我"保存到 Obsidian：[内容]"
2. **搜索笔记** → 告诉我"在 Obsidian 中搜索：[关键词]"
3. **查看 vault** → 文件已存在 `/home/admin/clawd/knowledge-base`

### 最佳实践
1. **使用有意义的文件名** → 例如：`编程开发/` 而不是 `1.md`
2. **使用 Markdown 结构** → 标题、列表、代码块
3. **添加元数据（可选）** → 可以在笔记中添加 tags、日期等
4. **定期同步** → Git 已经配置好，可以随时提交

---

## 🚀 当前限制

由于当前环境是 Linux 服务器：
- ❌ 无法自动打开 Obsidian 桌面应用
- ✅ 但可以使用 `obsidian-cli` 命令写入和搜索文件
- ✅ 文件会保存到 vault，下次打开 Obsidian 时可见

---

*最后更新：2026-02-03*
